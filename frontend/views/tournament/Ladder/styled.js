import styled from "styled-components/native"
import { CircleButton } from "../../../components/Form"

export const LadderWrapper = styled.ScrollView.attrs(() => ({
    contentContainerStyle: {
        padding: 40,
    },
    horizontal: true,
}))``
